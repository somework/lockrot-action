# hadolint configuration for the lockrot image.
ignored:
  # DL3018: `apk add` without pinned versions. Nothing is added; packages are only removed.
  - DL3018
  # DL3067: copying a whole filesystem from another stage. Deliberate: the pruned runtime is
  # flattened into `FROM scratch` so the removed files leave the image instead of only its top
  # layer. The copied stage holds no secrets and no build tooling.
  - DL3067
