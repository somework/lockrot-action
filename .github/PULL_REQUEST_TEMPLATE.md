<!-- What changes for someone using the action or the image, and why. -->

- [ ] `CHANGELOG.md` updated under *Unreleased* (skip for docs and CI-only changes)
- [ ] New `uses:` pinned to a full commit SHA with the version in a comment
- [ ] Inputs reach scripts through `env:`, never through `${{ }}` inside `run:`
